config RAM_BASE
	depends on VEXPRESS
	hex "RAM Memory physical base address"
	default 0x80000000

config RAM_SIZE
        depends on VEXPRESS
        hex "RAM memory size (Up to the hypervisor)"
        default 0x3f000000
        
