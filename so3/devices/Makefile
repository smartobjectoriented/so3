obj-y = device.o irq.o timer.o console.o

obj-$(CONFIG_UART) += serial.o

obj-$(CONFIG_MMC) += mmc/
obj-$(CONFIG_UART) += serial/

obj-$(CONFIG_RAMDEV) += ramdev/

obj-y += timer/
obj-y += irq/

obj-y += mydev.o
